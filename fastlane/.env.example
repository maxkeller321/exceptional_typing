# Fastlane Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit the .env file to git!

# ============================================
# APPLE CREDENTIALS
# ============================================

# Your Apple ID email (the email you use to sign in to Apple Developer)
APPLE_ID="your-apple-id@example.com"

# Your Apple Developer Team ID (10 characters)
# Find at: https://developer.apple.com/account → Membership details → Team ID
APPLE_TEAM_ID="XXXXXXXXXX"

# Your name as it appears on certificates
# Find at: https://developer.apple.com/account → Certificates → Click any certificate → "Certificate Name"
APPLE_TEAM_NAME="Your Name"

# ============================================
# APP STORE CONNECT API (Recommended)
# ============================================
# All values found at: https://appstoreconnect.apple.com → Users and Access → Integrations → App Store Connect API

# API Key ID (10 characters)
# Find at: Users and Access → Integrations → App Store Connect API → Key ID column
APP_STORE_CONNECT_API_KEY_KEY_ID="XXXXXXXXXX"

# Issuer ID (UUID format)
# Find at: Users and Access → Integrations → App Store Connect API → "Issuer ID" shown at top of page
APP_STORE_CONNECT_API_KEY_ISSUER_ID="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# Path to your .p8 key file (relative to fastlane directory)
# Download when creating the API key (can only download once!)
APP_STORE_CONNECT_API_KEY_KEY_FILEPATH="./AuthKey_XXXXXXXXXX.p8"

# OR use base64-encoded key content (for CI environments)
# Generate with: cat AuthKey_XXXXX.p8 | base64
# APP_STORE_CONNECT_API_KEY_KEY="base64-encoded-key-content"

# ============================================
# MATCH (Certificate Management) - Optional
# ============================================

# Private Git repository for storing certificates
# Create a PRIVATE repo, e.g., github.com/your-username/certificates
# MATCH_GIT_URL="git@github.com:your-org/certificates.git"

# Encryption password for certificates in the repo
# Use a strong password and store it securely!
# MATCH_PASSWORD="your-secure-encryption-password"

# ============================================
# OPTIONAL: NOTIFICATIONS
# ============================================

# Slack webhook URL for notifications
# SLACK_URL="https://hooks.slack.com/services/XXX/YYY/ZZZ"
